// Tailwind CSS v4 — CSS-first configuration
//
// All theme tokens (colors, fonts, animations, keyframes) are defined in
// app/globals.css using the @theme directive. This file exists for plugins
// and any v4-compatible imperative config that cannot live in CSS.
//
// Reference: https://tailwindcss.com/docs/v4-upgrade

export default {}
