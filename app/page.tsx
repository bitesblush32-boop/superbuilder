// Landing page — Super Builders × zer0.pro
// Section order per CLAUDE.md §16:
// Navbar → Hero → StatsBar → ProgrammePitch → Timeline → WorkshopCards →
// TierComparison → BadgeWall → DomainGrid → ForParents → JudgingCriteria → FAQ → FinalCTA

export default function LandingPage() {
  return <div>LandingPage</div>
}
