[build]
builder = "nixpacks"
buildCommand = "pnpm install && pnpm build"

[deploy]
startCommand = "node scripts/migrate.mjs && pnpm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
